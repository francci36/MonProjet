		// // Sélection du menu de navigation et de tous les liens
		// const ul = document.querySelector('ul');
		// const links = ul.querySelectorAll('a');

		// // Ajout de l'événement de clic à tous les liens
		// links.forEach(link => {
		// 	link.addEventListener('click', function(e) {
		// 		e.preventDefault();

		// 		// Suppression de la classe "active" de tous les liens
		// 		links.forEach(link => link.classList.remove('link-secondary'));

        //         links.forEach(link => link.classList.add('link-dark'));

		// 		// Ajout de la classe "active" au lien cliqué
        //         this.classList.remove('link-dark');
		// 		this.classList.add('link-secondary');
		// 	});
		// });